[project]
name = "travauxmetro.Fr"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "icalendar>=5.0.12,<8",
    "jinja2>=3.1,<4",
    "requests>=2.31.0,<3",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["tests"]
